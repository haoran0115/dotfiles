# MYDWM-6.4
Notes
```
# Add vanitygaps

# Let dwm always display status bar (https://dwm.suckless.org/patches/statusallmons/)

# Change window refresh/redraw rate
# in dwm.c, change 60 to refresh rate
if ((ev.xmotion.time - lasttime) <= (1000 / 60))
```
