local toggleterm = require("toggleterm")
toggleterm.setup()
-- toggleterm.setup({
--   shade_terminals = false,
--   highlights = {
--     Normal = {
--       guibg = vim.g.terminal_color_0
--     }
--   }
-- })

